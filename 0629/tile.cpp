#include <iostream>
#include <vector>

using namespace std;

int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    freopen("tile.txt", "r", stdin);

    int n;
    cin >> n;

    bool isEven = n % 2 == 0;
    
    
    
    return 0;
}