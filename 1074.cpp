#include <iostream>
using namespace std;

int N, r, c;
int result = 0;

void solve(int x, int y, int size)
{
	if(size == 1)
	{
		return;
	}

	int half = size / 2;


}

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	cin >> N >> r >> c;

	return 0;
}