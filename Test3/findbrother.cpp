#include <iostream>
#include <vector>
#include <queue>

using namespace std;

int main(void)
{
    freopen("findbrother.txt", "r", stdin);

    int N, K;

    cin >> N >> K;

    queue<pair<int, int>> q;
    int time = 0;
    int weight = 0;
    q.push({N, 0});

    while (!q.empty())
    {
  
    }

    return 0;
}